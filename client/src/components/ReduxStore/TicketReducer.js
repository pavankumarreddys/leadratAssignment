import React from 'react'
const initialState = {
    tickets:3
}

export const TicketReducer = (state=initialState,action) => {
    switch(action.type){
        
    }
  return (
    <div>TicketReducer</div>
  )
}
